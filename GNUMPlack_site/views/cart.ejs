<!DOCTYPE html>
<html lang="es">

    <%- include("./partials/head") %> 

<body class="body-cart">

    <%- include("./partials/header") %> 


    <main>
         <h3>Carrito de compras</h3>
        <div class="chosen-products">



            <% cart.forEach(e => { %>
                
               
            <div class="product-container">
            <div class="p-img">
                <img src="img/products/<%= e.product.image %>" alt="" srcset="">
            </div>
            <div class="p-details">
                <p><%= e.product.name %></p> <br>
                <p>Stock: <%= e.product.stock %></p> <br>
                <div class="p-amount">
                    <i class="fas fa-minus botonMenos menos"></i>
                    <p id="p-cantidad" class="p-contador">1</p>
                    <i class="fas fa-plus botonMas mas"></i>
                </div> <br>
                <!-- <sup>$</sup> -->
                <span class="p-price">Precio por unidad: $<%=e.product.price%></span>
            </div>
            <div class="trash-container">
                <form action="/cart/quitar/<%=e.product.id%>?_method=delete" method="POST">
                    <input type="submit" value="X"><!-- <i class="fas fa-trash"></i> -->
                </form> 
            </div>
        </div>
        
        <% }) %>
        
        </div>
       <!--  variable que contiene la suma de los precios -->
       <% let total = 0 %> 
        <!-- suma de los precios -->
        <% cart.forEach(e => { %>
         <% total += e.product.price  %> 
        <% }) %>
        <!-- pintando el precio -->
        <div class="total-container">
            <h4>El total es de:</h4>
            <p class="total" id="total">$ <%= total %> </p>
            
            <!-- <form action="/cart/comprar?_method=put" method="POST">
                <input id="submit" type="submit" value="Comprar!">
            </form> -->
            <div class="boton-modal">
                <label for="btn-modal">
                    Comprar!
                </label>
            </div>
        </div>
        <!-- ventana modal-->

        <input type="checkbox" id="btn-modal">
    <div class="container-modal">
        <div class="content-modal">
            <h2>¡Compra realizada con éxito!</h2>
            <p>Tu compra ha sido realizada exitosamente, por favor retirala en tu tienda GNUMPlack mas cercana:
                <a class="tiendaCercana" href="https://goo.gl/maps/QXuYudfsFKX7jdco6" target="_blank" rel="noopener noreferrer">Por aquí!</a>
            </p>
            <div class="btn-cerrar">
                <label for="btn-modal"><a href="/">Entendido!</a></label>
            </div>
        </div>
        <label for="btn-modal" class="cerrar-modal"></label>
    </div>

     <!--Fin de Ventana-->

    </main>

    <%- include("./partials/footer") %> 

    <script src="/js/cart.js"></script>
</body>

</html>