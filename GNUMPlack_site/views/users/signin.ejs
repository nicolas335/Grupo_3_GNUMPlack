<!DOCTYPE html>
<html lang="es">

    <%- include("../partials/head") %> 
    
<body class="signin">
    
    <%- include("../partials/header") %>
   
    <main class="signin">
        <h1> REGISTRARSE</h1>
        <form action="" method="POST" enctype="multipart/form-data" id="form">
            <h2 class="container bottom"></h2>
            <div class="container">
                <label for="name">Nombre</label>
                <input type="text" name="name" id="name" placeholder="Ingresa tu nombre" value="<%= locals.old && old.name ? old.name : null%>" >
                <small id="namesContainer"><%= (locals.errors && errors.name) ? errors.name.msg : '*' %> </small>
            </div>
            <div class="container">
                <label for="lastName">Apellido</label>
                <input type="text" name="lastName" id="lastName" placeholder="Ingresa tu apellido" value="<%= locals.old && old.lastName ? old.lastName : null%>">
                <small id="lastNameContainer"><%= (locals.errors && errors.lastName) ? errors.lastName.msg : '*' %></small>
            </div>
            <div class="container">
                <label for="email">Email</label>
                <input type="email" name="email" id="email" placeholder="Ingresa tu email" value="<%= locals.old && old.email ? old.email : null%>">
                <small id="emailContainer"><%= (locals.errors && errors.email) ? errors.email.msg : '*' %></small>
            </div>
            <div class="container">
                <label for="pass">Contraseña</label>
                <input type="password" name="pass" id="pass" placeholder="Ingresa tu contraseña">
                <small id="passContainer"><%= (locals.errors && errors.pass) ? errors.pass.msg : '*' %></small>           
            </div>
            <div class="container">
                <label for="pass2">Confirmar tu contraseña</label>
                <input type="password" name="pass2" id="pass2" placeholder="Confirmar contraseña">
                <small id="passContainer2"><%= (locals.errors && errors.pass2) ? errors.pass2.msg : '*' %></small>       
            </div>
            <!-- <div class="container">
                <label for="phoneNumber">Celular</label>
                <input type="number" name="phoneNumber" id="phoneNumber" placeholder="11..." value="<%= locals.old && old.phoneNumber ? old.phoneNumber : null%>">
                <small id="phoneNumberContainer"><%= (locals.errors && errors.phoneNumber) ? errors.phoneNumber.msg : '*' %></small>      
            </div>
            <div class="container">
                <label for="city">Ciudad</label>
                <input type="text" name="city" id="city" placeholder="Ej: Buenos Aires, etc" value="<%= locals.old && old.city ? old.city : null%>">
                <small id="cityContainer"><%= (locals.errors && errors.city) ? errors.city.msg : '*' %></small>                
            </div>
            <div class="container">
                <label for="gender">Género</label>
                <select name="gender" id="gender">
                    <option select hidden>Seleccione su género</option>
                    <option value="2" <%= (locals.old && old.gender === '2') ? 'selected' : null %>>Hombre</option>
                    <option value="1" <%= (locals.old && old.gender === '1') ? 'selected' : null %>>Mujer</option>
                    <option value="3" <%= (locals.old && old.gender === '3') ? 'selected' : null %>>Sin especificar</option>
                </select>
            </div>
            <div class="container">
                <label for="signinFile" >Añadir imagen</label>
                  <input class="signinFile" type="file" id="signinFile" name="imageUser" >
                  <small id="imagenContainer"><%= (locals.errors && errors.imageUser) ? errors.imageUser.msg : '' %></small>                
            </div> -->
            <h2 class="bottom">Terminos y Condiciones</h2>
            <div class="bottom">
                <input type="checkbox" name="terminos" id="terminos" <%= locals.old && old.terminos == "on"? "checked":"" %> >
                <label for="terminos">Al registrarte aceptas nuestras Condiciones de uso y Politica de privacidad</label>
                <small id="terminosContainer"><%= (locals.errors && errors.terminos) ? errors.terminos.msg : '*' %></small>
            </div>
            <div class="bottom">
                <input type="checkbox" name="sesion" id="sesion" <%= locals.old && old.sesion == "on"? "checked":"" %> >
                <label for="sesion">¿Desea mantener la sesión iniciada?</label>
            </div>
            <h2 class="bottom"></h2>
            <div class="bottom">
                <button type="submit" id="buttonSubmit">Registrarme</button>
                <button type="reset">Cancelar</button>
            </div>
            <!-- <div id="submitContainer"></div> -->

            <h4><a href="/inicia sesion">¿ya tienes cuenta? Inicia Sesion</a></h4>

        </form>

    </main>
    
    <%- include("../partials/footer") %> 
    <script src="/js/registerValidations.js"></script>
</body>

</html>